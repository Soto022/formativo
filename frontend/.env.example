# Este es un archivo de ejemplo para el frontend.
# Copia este archivo a .env, .env.development o .env.production y rellena los valores.
# En producción, esta variable debe ser configurada en el entorno del servidor/plataforma de despliegue.

# URL completa del backend API.
# En desarrollo, normalmente apunta a localhost.
# Para producción, debe ser la URL donde desplegaste tu backend.
# Ejemplo producción: VITE_API_URL=https://api-ecoalas.onrender.com
VITE_API_URL=http://localhost:5000
