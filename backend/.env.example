# Este es un archivo de ejemplo. Copia este archivo a .env y rellena los valores.
# En producción, estas variables deben ser configuradas en el entorno del servidor.

# -- Configuración del Servidor --
# Puerto en el que se ejecutará el servidor de la API.
PORT=5000

# -- Configuración de la Base de Datos --
# URI de conexión a MongoDB.
# Para producción, usa una base de datos remota (ej. MongoDB Atlas).
# Ejemplo producción: MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/database_name
MONGO_URI=mongodb://localhost:27017/ecoalas_db

# -- Configuración de Seguridad (JWT) --
# Secreto para firmar los JSON Web Tokens. Debe ser una cadena larga y aleatoria.
# Puedes generar uno en: https://www.lastpass.com/features/password-generator
JWT_SECRET=supersecretoseguroparajwt
# Expiración del token (ej. 1h, 1d, 30d).
JWT_EXPIRE=30d

# -- Configuración de CORS --
# Lista de dominios permitidos para hacer peticiones a la API, separados por comas.
# En producción, debe ser la URL de tu frontend desplegado.
# Ejemplo producción: CORS_ORIGIN=https://www.mi-app-ecoalas.com
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# -- Configuración del Chatbot (Ollama) --
# URL del host donde se ejecuta el modelo de lenguaje (Ollama).
OLLAMA_HOST=http://localhost:11434
# Nombre del modelo a utilizar.
OLLAMA_MODEL=llama3:latest

# -- Configuración de Logging --
# Nivel de logs a mostrar (error, warn, info, http, verbose, debug, silly).
LOG_LEVEL=info
